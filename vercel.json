{
  "version": 2,
  "builds": [
    {
      "src": "package.json",
      "use": "@vercel/static-build",
      "config": {
        "distDir": "dist"
      }
    }
  ],
  "installCommand": "npm install && chmod +x node_modules/.bin/vite",
  "buildCommand": "bash -c 'npm run build'"
}
